pipeline {
  agent any 
  options {
    skipStagesAfterUnstable()
   }
  stages {
    stage("Build") {
       steps {
          echo "Hello, Pipeline!"
          echo "Pipeline finished"
       }
   }
 }
}
